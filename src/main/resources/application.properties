# 数据源配置
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/smbms?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
#
spring.datasource.username=root
spring.datasource.password=root

#配置映射文件
mybatis.mapper-locations=classpath:mapper/UserMapper.xml

# 端口号
server.port=8880

#spring.mvc.view.prefix=/
#spring.mvc.view.suffix=.html

#mybatis.mapper-locations=classpath:mapper/*.xml

#为所有的实体类配置别名
mybatis.type-aliases-package=springboot.pojo